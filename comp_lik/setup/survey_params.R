# definitions of survey parameters
# stored here as its used in multiple loations

# survey selectivity parameters
cod_survey_alpha <- 0.25
flatfish_survey_alpha <- 0.25
capelin_survey_alpha <- 2
cod_survey_l50 <- 40
flatfish_survey_l50 <- 20
capelin_survey_l50 <- 8
survey_sel_max <- 0.01

# survey and fleet sampling errors
survey_error <- 0.2
catch_error <- 0.2